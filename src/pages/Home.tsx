import React from 'react';
import CepModal from '../components/CepModal';

function Home() {
  return (
    <main id="page-home">
      HOME
      <button type='button' onClick={CepModal}></button>
    </main>
  )
}

export default Home;